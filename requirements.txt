# Core GUI Framework
PySide6>=6.5.0

# Network and VPN
requests>=2.31.0
cryptography>=41.0.0  # Phase 2: X25519, HKDF, ChaCha20-Poly1305 (brief §0.3 allowed)
psutil>=5.9.0         # Phase 4: real process/thread timing for Amdahl demo (brief §0.3 allowed)

# Configuration and Data
PyYAML>=6.0
jsonschema>=4.19.0

# Logging and Monitoring
colorlog>=6.7.0

# Development and Testing
pytest>=7.4.0
pytest-qt>=4.2.0      # Qt-aware test utilities for GUI panel tests

# CN + OS Lab panels (declared now to avoid mid-phase requirements churn)
scapy>=2.5.0          # Phase 1: live packet capture delivery mechanism (brief §0.3 allowed).
                      #   Used ONLY as a byte-delivery source (sniff() raw bytes);
                      #   all decoding is hand-written in netlab/dissect/headers.py.
                      #   Requires Npcap on Windows for live capture; offline pcap
                      #   mode works without it (trap §7.1).
pyqtgraph>=0.13.0     # Phase 3: cwnd/RTT/sequence-ladder live plots (brief §0.3 allowed).
                      #   Chosen over matplotlib for zero-latency Qt-native rendering.
